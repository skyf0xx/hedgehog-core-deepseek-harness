# Copy to .env and fill in — DSH's dsh-credentials-local provider reads
# .env in the invocation cwd (the "project-env" layer) automatically.
# Needed from the `smoke` layer onward: `scaffold`, `logic`, and `wiring`
# don't boot a real DSH profile and don't need it.
DEEPSEEK_API_KEY=
